# Git

- Never push, force-push, rebase shared branches, or `reset --hard` unless explicitly asked in the current message.
- Commit only when asked. Stage specific files, not `-A`.
- Match the repo's message style from `git log`; when in doubt: short lowercase imperative subject.
- Do not amend a commit you did not create in this session.
